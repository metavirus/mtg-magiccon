# Artist directory monitoring repair

September 25, 2026 PDT; source and implementation verification September 26 UTC. The earlier source expansion watched `/en-us/guests.html`, not the released `/en-us/art-of-magic/artist-directory.html`. Its bounded link allowlist omitted `art-of-magic`, and the ordinary page fingerprint strips scripts. A successful static HTML check therefore did not cover the artist roster. The earlier heading-only review correctly avoided asserting an empty roster but did not provide a detector for its eventual dynamic publication.

The official directory embeds `growTixExhibitors` with public event key `d33e6b91-2a8f-4e35-970c-752c447b23ed` and category `20591`. Its own `/content/dam/sitebuilder/rna/innovations/magiccon-atlanta/2026/gt-scripts-min.js` constructs the LEAP `/api/space_orders?specials=1&key=...&category=...&search_term=` URL. A direct HTTP 200 read returned 65 entries, event ID `21389`, slug `htwhdatl26shdl10`, event name `MagicCon: Atlanta 2026`. This is the official widget's public data transport, not a guessed third-party roster. Source display spelling is retained (including Cynthia Shepard); canonical artist-name resolution belongs to the separately reviewed import.

`artist_directory_feed.mjs` binds the page's widget settings to the configured event/category, rejects empty, malformed, duplicate, wrong-category or wrong-event results, and fingerprints sorted artist identities, display names, booths, biographies, links, images, categories and tags. The runtime combines that roster hash with page content, retains all normalized artist evidence in changed-source reports, and changes the candidate fingerprint even when static HTML is unchanged. A transport/parser failure uses the existing source-failure path: partial coverage, nonzero check exit, no cloud acceptance. The compact `artistDirectoryCoverage` report field exposes checked count and roster hash even on quiet repeats. Shared-navigation coalescing cannot swallow a distinct roster change.

The watch set now includes the directory and Art of Magic overview, and `art-of-magic` links participate in bounded source-gap detection. The overview's exact reviewed snapshot is generic introduction plus the directory link, with no signing hours/prices or substantive attendee policy; its initial noise disposition retains evidence without duplicating the directory's Home announcement. The initial 65-artist snapshot has an exact content/link-bound Home editorial decision, using the existing finding, Home projection, closure and exact-report acceptance path. It cannot silently bootstrap or reuse that approval for changed bytes. Initial reviewed composite content hash: `1518d5fcc1d506b21074a951849f854e093c170a2ea86deb4ef86192fba655a2`; roster hash: `3de5fefca9ee6f892ae5a70942b75da23d367f0c1791d610a5836998db2be8a2`. No artist/database writer is added to the surveyor.

Local validation: targeted tests cover unchanged HTML with roster additions/removals/booth changes, ordering stability, event/category/widget identity, malformed/empty/failed feeds, source evidence retention, reviewed Home disposition, exact-report acceptance, link-gap detection and shared-navigation isolation. A separate read-only production-helper fetch confirmed all 65 entries and the configured hashes. No local operational check, stage, closure, or baseline acceptance was run.

Cloud activation is verified on commit `9a1b1c4`. Run `36212138777` completed successfully: 37 watched sources, two initial changes, zero fetch failures, complete 65-entry artist feed coverage with the reviewed roster hash; overview ignored as reviewed noise and directory routed to Home. Exact app-projection readback verified “Atlanta's artist directory is available,” closure completed, and the exact report baseline was accepted and saved. Authenticated browser Home readback also confirmed the card.

Quiet repeat `36212302077` completed successfully at 02:39 UTC September 26: zero changes, zero failures, all 65 feed entries checked, complete closure, baseline accepted/saved, and no duplicate artist catch. Overall `coverageStatus` is still `partial` because of the separate Gathering Grounds schedule gap below. These runs prove artist monitoring, not complete monitoring of every dynamic source. Gmail was not checked in this artist-release task.

## Remaining Gathering Grounds coverage

The current Gathering Grounds page links `/en-us/experience/the-gathering-grounds/the-gathering-grounds-schedule.html`. That child embeds `growTixScheduleSlim`, the same public event key and category `20600`; direct HTML contains only the schedule heading. Its schedule feed needs a separate bounded adapter/review. The current link-gap detector already recognizes this URL and must retain it as unresolved coverage until actual schedule data is monitored. Do not add a heading-only watch or exclusion to clear the gap. The artist repair does not certify dynamic schedules, guests, creators, or merchandise on other pages.
