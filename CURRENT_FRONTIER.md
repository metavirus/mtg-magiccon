# Current Frontier

Updated: 2026-08-24

## Flight signal closed loop

Hosted migrations `20260822221053_canonical_trip_flights_and_surveyor_apply` and `20260822221437_harden_trip_flight_evidence` establish the shared canonical Atlanta Delta itinerary and current legs, private immutable source evidence, its required FK index/explicit deny policy, and a service-role-only auto-apply function. A complete routine schedule change now requires a confident match on confirmation HOGFBX, Delta, and Kavi + Juan; it atomically updates Trip data and emits one concise unread Home concept. Cancellation, rebooking ambiguity, identity mismatch, low confidence, or incomplete changed-leg facts fail closed. The active `magiccon-atlanta-quiet-period-monitor` heartbeat now follows detect → extract → reconcile → apply → brief signal → done and is prohibited from turning a confidently matched flight update into a generic Codex review request. The actual Aug 22 email has not yet been applied because its changed leg facts were not present in the screenshot; the updated heartbeat must re-read that source and use the bounded executor.

Migration `20260822222758_enforce_flight_update_consequence_guards` first corrected the original prompt-only executor boundary by moving consequence checks into a service-only database wrapper and removing callable access to the internal executor. Its blanket non-rebooking rule is superseded by the narrower airline-assigned-replacement exception below; explicit completeness and classification remain mandatory.

Migration `20260822223827_allow_guarded_airline_assigned_replacements` adds the narrow safe replacement case: an airline-assigned replacement may run only with explicit no-action/no-choice and same itinerary/travelers/carrier/dates/routes evidence. Gmail message `1a02b0ef27bc3abf` was applied through that guard: outbound is now DL 329, SNA 2:00 PM → ATL 9:16 PM on Nov 11; return DL 1602 is ATL 8:25 PM → SNA 10:14 PM on Nov 15. One immutable evidence row, one `Your Atlanta flight changed.` signal, and one unread Home concept exist; repeating the message returns `already_applied`.

`docs/ROUTINE_FINDING_AUTO_APPLY_POLICY.md` extends the same bounded rule to matched hotel confirmation fields, official event sold-out/reopened status, and maintained official hours/details: confidence, stable binding, deterministic fields, reversibility, retained evidence, and no contradiction/destructive consequence are all required. `src/lib/routineFindingPolicy.ts` provides the regression-tested decision boundary. Trip now loads `trip_flights` / `trip_flight_legs` and renders canonical flight times; preview fixtures remain only the offline/design fallback.

## Semantic Map planning checkpoint

`docs/SEMANTIC_MAP_MINIPROJECT_PLAN.md` preserves the design history, while `docs/MAP_INGESTION_RUNBOOK.md` is now the single clean arrival procedure. The runbook begins only after a first-party original Atlanta 2026 artifact passes event/year/venue/floor/hash checks. It then owns evidence capture, independent 2026 orientation, reviewed visitor-meaningful deconstruction, zoom density, search and domain bindings, data validation, later app integration, offline packaging, and deployed device proof. Obsolete routing, origin, camera, live-position, and accessibility-path instructions were removed rather than left behind as misleading ceremony.

`docs/MAP_SEMANTIC_DATA_CONTRACT.md` now freezes the narrower active model behind the accepted prototype: explicit levels and containment, simplified reviewed geometry, visitor-meaningful access points, zoom-dependent presentation, deterministic search, cross-surface bindings and return context, and private personal overlays. Proximity never implies parentage, unbound domain objects never pin, and routing/camera/live-position records are absent from the active contract. `MAP-NOW-01D` and `MAP-NOW-01` are complete: `docs/map-data/` contains machine-checkable schemas, a deliberately synthetic fixture, and semantic validation. Nothing under `src/` imports it, so the app remains unchanged until a reviewed official map exists.

`MAP-NOW-02` and `MAP-NOW-03` are also complete. `docs/map-data/historical/atlanta-2025/reference.json` quarantines the prior edition without pretending the original artifact is stored, and `pnpm validate:map-data` now proves a clean future-edition control can activate while eight unsafe historical/provenance/revision/review/containment/binding/routing mutations fail. Source references carry their actual edition namespace, which is the enforceable barrier against quietly reusing old coordinates or labels.

Atlanta 2025 remains structural reference only. Its west-up orientation is user-confirmed for that edition and must never seed a 2026 orientation claim; no original-resolution 2025 artifact is currently tracked in this repository. The backlog now decomposes the work into executable `MAP-NOW-*`, `MAP-ARR-*`, `MAP-ON-*`, and `MAP-LATER-*` IDs with dependencies, owner lanes, acceptance criteria, and an explicit safe-now/must-wait boundary. No Map feature, schema, canonical spatial data, or public behavior changed in this planning lane.

Pre-arrival Map work is now parked by product decision. `MAP-NOW-00` through `MAP-NOW-03` are the completed preparation boundary; remaining prototype/tooling ideas must be reconsidered against the real artifact rather than executed speculatively. Nothing in `docs/map-data/` is imported by the app.

The parked inventory is explicit in `docs/WORK_BACKLOG.md`, with intent preserved rather than flattened. Small per-item inbox dismissal and a recoverable collapsed dismissed group are planned now. Bell movement is separately planned for convention time only, when a genuinely urgent signal such as official-map publication or an onsite change exists; nothing currently merits motion. Show Store, Marketplace/exhibitor, and Art of Magic close views are planned once official layouts exist. Offsite-event handling is artifact-dependent because the event itself is not yet confirmed. Cohesive wayfinding/camera/origin work and optional rotation/compass/depth/control polish remain “consider later” experiments.

## Info Recent recurrence correction

Hosted migration `20260822161827_info_feed_concept_current` replaces append-only Recent cards with stable `concept_key` identity, a `current|superseded|internal` lifecycle, and one current row per concept. Live evidence is preserved without fake decisions: the comprehensive Prize/ODE/Ticketed rows are current, the link-only resource row is internal, and prior/bookkeeping summaries are superseded. Ingestion and surveyor projection now upsert current concept state, while the reader independently collapses by concept as a recurrence guard.

## Latest completed lane — surveyor closed loop

Info now uses maintained comprehensive articles rather than link summaries. Hosted migration `20260822154609` adds the incomplete/maintained gate, structured article payload, and forced-RLS source snapshots. Four official Magic Play pages were captured as URL/status/retrieval/hash evidence without storing full copyrighted bodies; retained official order-confirmation evidence supports Show hours and Will Call. All five Info topics are maintained with factual sections, unknowns, provenance, and recent changes; the Sunday ODE timing tension is explicit. Link-only monitoring projection fails closed and cannot publish a finished article. The deterministic seed reran without duplicate snapshots, articles, or feed entries; no monitoring baseline or unrelated canonical fact changed.

Map and Info are now separate product destinations. Hosted migrations `20260821212655` and `20260821213154` add forced-RLS, active-companion-readable maintained topics and a persistent official-information feed, with a narrowly granted trusted writer lane. Show hours is the lead quick answer; Will Call, Ticketed Play, On-Demand Play, and Prize Tix are source-backed topics. Surveyor noise/corroboration stays out of the feed, while deterministic meaningful concept resolutions persist once and update a maintained topic only when explicitly mapped. No baseline was accepted.

The concept-diff layer is now the monitoring read-model boundary. Hosted migrations `20260821210125`, `20260821210412`, and `20260821210653` add Kavi-only `monitoring_concepts`, evidence lineage, the required FK index, and persisted unread/read/archive review state with forced RLS. Deterministic versioned rules classify noise, corroboration, new concepts, material updates, contradictions, and milestone transitions. Raw source diffs remain internal; one stable concept renders once, and keyed legacy alerts yield to the live concept. The existing Magic Play resource finding backfilled as one informational concept with five labeled resources and one evidence link; no canonical fact or monitoring baseline changed.

Commit `f0fe3f1` (`Close the reviewed surveyor action loop`) is on `main`. CI run `32520980614` and Pages run `32520980683` succeeded with zero annotations; `pnpm verify:public` passed, and the deployed Activity flow passed desktop and mobile visual inspection. Manual surveyor runs `32521125551` and `32521224614` both succeeded. The workflow-owned fingerprint beginning `bc7e5a` remained one row and its `occurrence_count` advanced to `2`, proving idempotent deduplication. The obsolete undecided prototype row beginning `fdb94` was deleted.

The informational-finding correction is live. Migration `20260821202823` reclassified the workflow-created Magic Play link appearance as `unread` with preserved evidence/dedupe history and no action or decision audit. Commits `0af0be5` and `0a8d0a7` shipped the data/router/UI correction and removed the remaining raw link-delta presentation. CI `32524396031`, Pages `32524396062`, `pnpm verify:public`, and deployed desktop/mobile Home, Activity, and drawer inspection passed. The UI shows labeled official resources, `Status`, and read/archive controls; it shows no raw URL dump, `Decision`, `Target`, or `Publish` action. No monitoring baseline was accepted.

## Where we are

- The surveyor queue preserves fingerprint-deduplicated source evidence. Official-link appearances are informational findings with read/archive state; they do not create a second Activity object, request approval, or mutate canonical facts. Consequence-aware authorization remains reserved for future findings with a real, named canonical effect. Kavi-only forced RLS, Chris denial, clean advisors, cloud staging, repeated-fingerprint dedupe, CI, Pages, public verification, and deployed desktop/mobile inspection are proven. No monitoring baseline was accepted.

- Artist/card signing data has moved out of CSV-first staging into a canonical normalized Supabase catalog. Migration `20260820173320_artist_card_canonical_catalog.sql` owns artists, Atlanta appearances, card identities, owned printings, style/taste assessments, import provenance, and per-user signing interests. Live Supabase readback on August 20, 2026 shows the full local corpus imported: 1,122 artists, 5,068 cards, 5,451 printings, 5,451 assessments, and 4 Atlanta appearance rows: Cynthia Sheppard, Mark Poole, Serena Malyon confirmed; Rebecca Guay unconfirmed. The Artists UI now reads this catalog first; enriched CSVs and generated local modules are source/import evidence or offline fallback only, not app truth.
- The public preview has completed a share-readiness cleanup for Chris: representative Vegas events, fake Wallet proofs/receipts, routing-demo Activity alerts, speculative artist cards, and design-note fixtures are no longer exposed. Empty surfaces now state the real waiting condition; confirmed Atlanta Black Lotus, trip, badge, and forecast facts remain.
- Greenfield mobile PWA and operating documentation are established.
- Supabase identity is fixed to `pavjsexxbueuzhzgemgy`.
- The first migration proves one owner-scoped private record with explicit grants and RLS.
- Offline behavior is intentionally read-only.
- Development is hosted-first: guarded Supabase CLI plus Session Pooler verification. Docker, WSL, and a local Supabase replica are not project prerequisites.
- Initial problem-space research is recorded in `research/PROBLEM_SPACE_EXPLORATION.md`. Vegas 2026 is a structural reference only; Atlanta 2026 remains the target convention and the source of future normalized facts.
- Pre-ticketed-play discovery is synthesized around a stable interface, quiet watch-duty relief, rare meaningful alerts, collapsed observations, trip/wallet reference views, and small situated quality-of-life affordances.
- `research/PRE_TICKETED_PLAY_HYPOTHETICAL_HYDRATION.md` is captured as a bracketed paper artifact only; it is not a seed plan, schema, monitor, or production-data proposal.
- Ticketed-play discovery is synthesized around information translation, owner-centered relevance triage, nonrefundable commitment states, fixed versus flexible time semantics, sellout/change monitoring, and person-aware but owner-managed planning.
- Cross-phase discovery now identifies the main product shape: contextual retrieval across fragmented sources, milestones as the quiet-period landing spine, MagicCon-specific friction only, robust preparation with lightweight use, first-class places, over-ingested store/prize surfaces, Wallet as operational reference, contextual notes, prudent offline cache hygiene, and post-event retrieval after neglect.
- The next phase is scoped in `docs/NEXT_PHASE_SCOPE.md`: design the persistent cross-phase experience, settle decision/time/source language, test priority surfaces on mobile and desktop, and implement one narrow Black Lotus trust slice with change reconciliation and offline reading.
- The owner accepted the direction of `docs/EXPERIENCE_ARCHITECTURE.md` and refined it toward focused access: Home, Calendar, Plan, Explore, Map, Wallet, Trip, and Notes are primary desktop destinations; Activity is a directly accessible utility with All, Changes, Sources, and Personal streams; Now is Home's phase-aware focus. The visual direction is a dark, layered convention-command-center language with semantic color, shading, and icons. Exact mobile navigation remains a screen-design question.
- The planning-language gate in `docs/PLANNING_INTELLIGENCE_MODEL.md` is accepted: Tentatives form a synthesis layer; only overlapping fixed Commitments create a hard conflict; purchase staples an event into Plan; conditional overlap and schedule pressure remain advisory; normal source facts stay visually quiet; changes use graded attention; contradictions preserve both claims and may solicit a definitive personal operating choice. Voluntary onsite reporting is not an MVP dependency.
- Calendar and Plan are separate zoom levels. `docs/CALENDAR_SURFACE_CONCEPT.md` defines Calendar as a primary scrollable meaningful-date stream spanning milestones, deadlines, travel, Black Lotus preview day, convention summaries, and limited follow-up. `docs/PLAN_WORKSPACE_CONCEPT.md` now defines one Plan workspace for November 13–15 contention rather than separate Decide and Schedule modes. Convention-day clusters expand inline in Calendar and deep-link into the selected Plan day.
- `docs/DESIGN_STATE_ASSESSMENT.md` records the earlier prototype audit. Its split Plan playground is historical interaction evidence only; the accepted direction is now the single shared workspace recorded in `docs/PLAN_WORKSPACE_CONCEPT.md` and the design baseline.
- Plan screen studies now support the accepted visual grammar: category tint remains stable; Interested is quiet with a dotted border; Tentative restores full category fill while retaining the dotted border; Committed is solid; cool blue is focus only; gold is purchase only; an original lotus glyph identifies Black Lotus; and compact person markers encode identity by fill and involvement by border without introducing multi-user behavior. Purchase and planning-state actions are reversible.
- The canonical responsive Plan direction is established. Desktop keeps a dominant shared time field and opens event detail as an overlay drawer so later hours do not compress; mobile uses a thin time ruler plus readable event rows and a bottom detail sheet. Selected detail uses concise copy and suppresses controls or explanations that are obvious or inapplicable.
- Interested events remain quiet full temporal blocks in Schedule rather than collapsing to point markers, because their duration and overlap are part of the planning value. No extra “show interests” mode is warranted yet.
- Initial format-complexity research in `research/FORMAT_COMPLEXITY_EXPLORATION.md` separates rules novelty, preparation burden, live mental load, and competitive stakes. The event-detail thermometer may summarize those dimensions, but must expose its rationale, accept event-level overrides, and keep personal attractiveness separate.
- Map has been recalibrated around two top contexts. Its quiet-period landing is a **Trip area** orientation using confirmed lodging and venue facts; **Event map** remains a separate evidence-led context that becomes useful when official 2026 floor artifacts arrive. The current confirmed lodging facts are Omni Atlanta Hotel at Centennial Park for Nov 12-15 and Courtyard by Marriott Atlanta Downtown for Nov 11-12. Sensitive booking values such as PINs remain deliberately hidden.
- Map's earlier MVP-scaffold/v2-atlas umbrella has been replaced by the executable Semantic Map miniproject. The live quiet-period surface remains honest and unchanged, while safe-now contracts/tooling may prepare clickable overlays, reviewed rough routes, bindings, offline bundles, and approximate-anchor UX against quarantined fixtures. Atlanta 2026 layout, orientation, zones, booth facts, graph, joins, anchors, and active bundle still require dated first-party 2026 evidence plus runbook review.
- `docs/DESIGN_AUDIT_2026-08-02.md` critically reconciles the screen studies with the evolved product model. The concepts are locally useful but do not yet prove one coherent product. Remaining design should prioritize a shared shell, contextual find and backlinks, reversible state propagation, object-aware offline language, a canonical Plan scenario, the Explore-to-Plan bridge, and a Map/Trip/Wallet retrieval cluster before implementation contracts are derived.
- `docs/DESIGN_BASELINE_2026-08-03.md` completes that coherence pass. The accepted studies now read as one product across Orient (Home/Calendar), Choose (Explore/Plan), Retrieve (Map/Trip/Wallet/Notes), and Trust (Activity/evidence). The baseline records the shared shell, responsive behavior, semantic color, reversible planning states, person markers, AI restraint, offline/freshness language, and MVP/v2 boundary.
- Quiet Home, Calendar density, Explore-to-Plan, responsive Plan, translated Event detail, Map/Trip orientation, Wallet proof retrieval, and Notes/Activity paths have credible accepted first-pass directions. Exact mobile bottom navigation remains deliberately unresolved and should be tested against the working trust slice rather than settled by another isolated mockup.
- An original peach wordmark and iOS home-screen icon now establish local project identity. Apple touch-icon wiring is present in the application shell; compact silhouette treatment still needs real-device legibility testing.
- The production React surface is still the intentionally narrow owner-auth/RLS/offline-contract foundation. No visual study has silently become implemented product state or broadened the database scope.
- The first Black Lotus trust slice is now live in canonical Supabase. `sources`, `source_observations`, `occurrences`, `personal_decisions`, and `itinerary_entries` are owner-scoped with forced RLS, explicit authenticated grants, owner-matched relationship keys, and no anonymous grants. Migration `20260803173516` is applied and in parity.
- One official Atlanta Black Lotus observation retrieved August 3 preserves the published Saturday Planechase Unknown wording, 11:30 AM-3:00 PM occurrence, Commander-deck preparation clue, location uncertainty, and schedule-change caveat. Interested state and the active Plan placement remain separate owner records.
- The React app now presents the narrow trust slice in a dark responsive Home/Plan/Evidence shell. The live owner-auth/RLS path remains proven in Supabase. Google OAuth is configured against the canonical Supabase project `pavjsexxbueuzhzgemgy`; desktop Pages auth now signs in as `kavigrace@gmail.com`, shows the account chip, and persists after refresh. Magic-link UI is parked because it caused email quota friction. As of August 9, normal app entry is auth-first; `?preview=1` is the explicit developer fixture bypass rather than the default review mode. Setup and acceptance evidence are recorded in `docs/GOOGLE_OAUTH_SETUP.md`.
- The live density audit now establishes a left-anchored fluid desktop canvas and a separately composed 390px mobile flow. Compact rules load after the base stylesheet; ordinary copy remains readable while chips, state controls, cards, and consequence blocks avoid POC-scale padding. `docs/POC_BUILD_TRANCHES.md` defines the review-gated implementation sequence.
- The tranche sequence is now active. Home presents a calm quiet state, countdown and last-check context, the next meaningful milestone, a compact full runway, and the captured Black Lotus anchor. The complete destination architecture remains directly reachable on desktop; the phone candidate uses a horizontally scrollable destination strip rather than collapsing the product into five destinations.
- Tranche 1 is now accepted after compact-header, vertical-milestone, identity-chip, favicon, and consistent vector-navigation refinements. Tranche 2 Calendar is accepted: meaningful dates form a compact chronological stream, quiet weeks compress, travel/lodging and Black Lotus context remain visually distinct, and the November 14 cluster expands inline before deliberately handing off to Plan. The trip rows now carry the most important flight and hotel timing rather than duplicating travel rows: Nov 11 shows Delta arrival plus Courtyard, Nov 12 shows Black Lotus First Look plus Omni check-in/luggage transition, and Nov 15 shows Omni checkout plus the Delta return flight with a future leave-for-airport reminder hook.
- Calendar is now an interaction surface rather than a dead summary. Upcoming/Past modes, a real completed badge-sale milestone linked to the official purchasing page, contextual milestone and travel details, clickable convention days, a responsive event-detail drawer/sheet, reversible planning states, deliberate removal from Plan, and direct Plan handoff are implemented against the representative fixture. Purchased history remains intentionally separate from planning state.
- Tranche 3 Explore is implemented for live review. It remains fixture-backed and visibly representative rather than ingested canonical Atlanta data. The surface now proves compact browsing, search/day/mode controls, For you / All / Changed / Hidden views, icon-first Interested / Tentative / Hidden / Not for me actions, concise owner-fit rationale, source-informed complexity labels, a desktop detail pane, a mobile browse-first detail sheet, and Plan handoff for events worth comparing.
- Plan has a legitimate Plan-lite surface with shared Supabase-backed decisions and contextual detail. The production contention engine stays parked until purchases/locked commitments create real planning conflicts; Plan-lite is sufficient unless bought/locked events make conflict resolution urgent.
- The retrieval cluster has started with working Trip and Wallet surfaces. Trip now has compact Hotels and Flights subtabs. Hotels uses a source-led lodging flow rather than a generic travel dashboard: Kavi, Juan, and Chris share the Courtyard on Nov 11; Kavi and Chris attend Black Lotus First Look on Nov 12; Kavi and Juan then stay at the Omni through Nov 15 while Chris branches to an as-yet uncaptured hotel. Official hotel pages verify the displayed addresses and Omni's 4 PM check-in / 11 AM checkout. Flights uses bounded Gmail fixture evidence from Delta operational receipts: Kavi and Juan share confirmation HOGFBX, fly SNA to ATL on Delta 1521 on Nov 11, and return ATL to SNA on Delta 1602 on Nov 15. A restrained transition insight identifies Thursday luggage handling as the only trip issue worth settling now, and the flight check stays quiet unless a consequential Delta change appears. No booking-management system, flight tracker, monitor, or new schema was added.
- Wallet is implemented as a fixture-backed POC with Home, Play, Store, and Other subtabs. Play owns ticketed-play/event receipts because event codes, pickup proof, and original receipts are likely the highest-value onsite artifacts. Store proves extracted line items, quantities, notes, catalog backlinks, and quick assignment chips using the Vegas #39Z8 receipt. Other holds travel, hotel, Black Lotus badge-order receipts, and miscellaneous confirmations. Wallet Home now uses the deliberately simple Prize Tix counter; the decorative ticket/drawer idea is parked. The Black Lotus order proof is no longer fake: the app exposes the Leap order, Wallet links, original email reference, and an ingested QR crop from the Gmail order confirmation behind the normal Google-login app flow. Later private Storage is still the cleaner artifact model, but it is not required before this personal-app preview can be useful.
- Wallet share-readiness now has a more practical retrieval rule: removing fake fixture clutter must not remove real proof usefulness. Badges stay on Wallet Home rather than becoming a separate top tab. The Black Lotus badge pills and receipt-feed row open the same proof modal, and that modal has `Info` and `Original` modes. `Info` is the fast-use view with extracted logistics and clean QR/order-summary clips. `Original` means original: a full, scrollable Gmail receipt render, including whatever billing/shipping context appears in the original receipt, because the purpose is to satisfy staff who may ask to see the actual email. Juan's Premium Weekend badge now has the same clickable Home proof path with Juan's global person bubble, extracted order facts, the Gmail confirmation code, the email-derived order QR URL, and a full Gmail-rendered receipt artifact.
- Person involvement markers are a universal visual language, not destination-specific content. Use small overlapping colored circles (`Ka`, `J`, `C`, future `Ky`) in header/corner positions, matching Trip; do not consume whole cards, stat boxes, or pill rows just to show who is involved.
- Contextual notes now have a documented direction in `docs/CONTEXTUAL_NOTES_MODEL.md`. Notes are a universal object-attached layer, not separate note systems for Trip, Wallet, receipts, events, artists, Map, or Activity. Surfaces may render different note-box densities, but they share the same author bubble, timestamp, visibility, object reference, archive, and filter semantics. Notes attach to the narrowest useful anchor and roll up into Notes as an index. New notes are shared with authenticated collaborators by default; an unchecked-by-default `Private` control makes a note owner-only. Only the owner may edit or delete a note.
- The first contextual-notes implementation is now live as one Supabase-backed collaboration layer. Wallet proofs, Plan, Explore, and generic object drawers use the same default-shared composer; Notes and Activity read the same collection across devices. RLS exposes shared notes to authenticated collaborators while preserving owner-only private notes and owner-only mutation.
- User selections are now mandatory Supabase state rather than browser-local state. Event interest/tentative/committed/hidden/not-for-me choices, Activity review status, and the Prize Tix counter persist through `user_selections` with forced RLS and explicit authenticated grants. Active companions may read only one another's event planning-state rows so Plan and Calendar can show real multi-person overlays; all non-event selections and every mutation remain owner-only. Local storage remains only for UI chrome/auth-mode convenience and the read-only trust-slice cache.
- Receipt handling now has a documented ingestion contract in `docs/RECEIPT_ARTIFACT_INGESTION.md`: when a MagicCon receipt is found, capture a durable original proof artifact and extracted facts once, then render Wallet/Activity/Home proof surfaces from that stable object. Gmail should be an ingestion and monitoring source, not something the UI has to revisit during ordinary interface work. The POC can temporarily store artifacts in the public preview bundle; the 1.5/v2 target is private Supabase Storage without changing the receipt object shape.
- Operational logistics are not Wallet-only facts. Will Call, show hours, and play-area hours are surfaced as reusable object details from Home/known-plan context, while Wallet keeps the original badge proof and receipt trail.
- The Explore event-detail decision-fact grid now suppresses the awkward empty Prize Tix cell when fixture data has an odd number of facts. This keeps unsupported prize metadata visibly absent instead of implying a missing source value.
- A final POC cohesion pass on August 4 clicked through Home, Calendar, Plan, Explore, Map, Wallet, Trip, Notes, and Activity at desktop and phone-sized viewports. The main surfaces render without auth blocking, broken images, or broad horizontal overflow. Map is no longer a dead rail destination: it now has a deliberately honest quiet-period landing that orients around Courtyard, Omni, and the convention center while reserving official floor-map/booth intelligence for later evidence. Wallet Home still shows phone-width strain around the Prize Tix ticket and should be redesigned mobile-first in v1.5 rather than patched cosmetically. Activity works as a review stream but will need mobile filtering/density attention once real monitor output accumulates. Button-driven navigation is verified; URL-deep-link/hash behavior should be treated as a v1.5 polish decision rather than a POC blocker.
- Historical POC checkpoint: the monitoring-alert landing UI was first proven through fixtures before a durable inbox existed. Home/Activity/Notes routing remains valid, but authenticated cloud-surveyor evidence now belongs in `monitoring_findings`; fixture intake is only the preview fallback.
- A temporary GitHub Pages preview is now available for iPhone review at https://metavirus.github.io/mtg-magiccon/. It remains a lightweight hobby-app preview rather than a production deployment. Google OAuth is now the deliberate live-auth path, while fixture/preview mode remains available for unblocked review when needed.
- `docs/POC_FINISH_GLIDE_PATH.md` now defines the remaining POC glide path and the routing contract for future agent-discovered information. Discoveries should land by usefulness rather than source system: rare consequential signals can reach Home, source/change history belongs in Activity, personal thoughts belong in Notes, proofs and receipts belong in Wallet, travel facts belong in Trip, event options belong in Explore/Plan, dated commitments belong in Calendar, and location artifacts belong in Map.
- The preview includes representative landing-route fixtures for a ticketed-play/news milestone, Black Lotus page change, receipt import, Delta travel change, artist-list opportunity, and official map unlock. `docs/MVP_MONITORING_AGENT_DESIGN.md` defines the monitoring boundary. The active heartbeat remains read-only toward Gmail and other source systems, but it may now use the dedicated service-only flight executor for confidently matched routine HOGFBX schedule changes; generic canonical writes remain prohibited.
- `research/MONITORING_SOURCE_STRATEGY_2026-08-04.md` now defines the monitoring source hierarchy. Canonical watch sources are Kavi's MagicCon-related inbox notices, the full Atlanta official site tree, the Atlanta Black Lotus page, the global MagicCon news feed, and Leap surfaces when available. Prior Vegas/Amsterdam/Atlanta pages justify additional watch surfaces for maps, mobile app data, show store/catalog, prize wall, artists, exhibitors, meet-and-greet wristbands, and Pastimes format interpretation. Broader search, social, vendors, and Reddit are radar only, not canonical truth.
- Legacy preview lane: `public/monitoring-intake.json` remains a fixture/QA seam and fallback only. It does not define current cloud behavior. The GitHub surveyor stages noncanonical evidence into `monitoring_findings`; the separate Codex heartbeat has only the narrow, deterministic flight-update exception documented in `docs/FLIGHT_CHANGE_AUTO_APPLY.md`.
- The ticketed-play timing chip is deliberately a forecast, not canonical event data. A bounded Gmail evidence check found the February 3, 2026 Vegas announcement that ticketed play would go on sale the following week for a May 1 convention; Home translates that single historical clue into a clearly labeled mid-to-late-August Atlanta estimate. It must update when Atlanta-specific publisher evidence appears.
- Home and Calendar now share the same compact milestone forecast definitions, preventing their timing language from drifting. `research/MILESTONE_FORECASTS_2026-08-03.md` records concrete windows and evidence: ticketed play Aug 18-25, artist directory Oct 9-16, Black Lotus online store Oct 30-Nov 3, and show catalog Oct 29-Nov 6. These remain explicitly noncanonical forecasts and are replaced when Atlanta publisher evidence arrives.
- After the August 3 Codex desktop restart, ordinary `node`, `pnpm`, and `pnpm readiness` work from the normal Windows `PATH`. The readiness script still contains the bundled-runtime fallback for resilience, but Node PATH is no longer an active recurring issue. The managed-sandbox Vite/esbuild config-resolution failure is fixed by running Vite and Vitest with `--configLoader runner`; plain `pnpm build` and `pnpm test` now pass inside the sandbox without escalation.
- Readiness now falls back from unauthenticated Supabase CLI migration listing to a Session Pooler read of `supabase_migrations.schema_migrations`, while still requiring the canonical project ref and hosted RLS/grant proof. This avoids making a separate CLI access token a prerequisite for ordinary UI tranches.
- Historical auth note: magic-link diagnosis on August 3-4 confirmed successful owner logins and no server-side logout or revocation, but Supabase email quota made magic link a poor POC testing loop. That parked magic-link path has been superseded by Google OAuth as the live-auth path. Do not revive email magic-link testing casually or describe auth as hard-disabled.
- `docs/V1_5_PATH.md` records the post-POC path. Product-coherence, auth continuity, and the reviewed monitoring inbox now exist. Production Plan remains parked until purchases/locked commitments create real conflicts; do not use the mere existence of ticketed-play data as the trigger.
- The first v1.5 coherence slice is implemented in the fixture-backed app. Home now starts with a command-center attention strip that answers whether anything needs review, then routes the strongest open signal into a shared object detail or Activity. Activity is reframed as a review inbox with Hot, All, Changes, Notes, and System streams. Meaningful review/ignore state belongs in Supabase-backed user state, not per-device browser storage. A shared object-detail drawer/sheet now supports representative event, alert, receipt, hotel, note, and activity objects across Home, Explore, Wallet, Trip, Notes, and Activity, preserving source/rationale/backlinks without replacing all destination-specific panels yet.
- The shared object-detail grammar received its first 1.5 polish pass. Detail drawers/sheets now carry a compact object-type chip, clearer source/provenance labeling, sticky destination/backlink actions, and less generic Activity button language. This strengthens the single-product feel without expanding schema, auth, or Plan scope.
- Home's command-center treatment now separates status from inspection. The top strip says whether the world is quiet or review is needed, and a distinct top-signal card appears only when there is a meaningful open finding. The milestone runway and known-anchor modules remain below as quiet-period context, with no new monitoring, schema, auth, or notification scope added.
- Wallet Home is now mobile-first rather than ticket-art-first. Prize Tix is deliberately reduced to a compact counter beside the Home / Play / Store / Other subtabs, with immediately visible `-100` / `+100` controls and no drawer. The decorative ticket treatment remains parked design evidence for a later onsite refinement; it is not active MVP UI.
- Mobile navigation now uses five fixed destinations instead of a horizontally scrolling rail: Home, Events, Map, Wallet, and More. Events opens a compact Explore / Plan / Calendar switcher; More holds Trip / Artists / Notes / Activity. Desktop retains direct access to every destination. This preserves the distinct product surfaces while keeping all phone navigation visibly reachable.
- Activity has moved from a placeholder intake explanation toward a usable monitor inbox. It now foregrounds reviewable findings, count-aware Review / Changes / Sources / Notes / Archive streams, compact alert cards, reversible review/archive actions, and contextual object opening. The monitoring contract and Black Lotus proof are still present, but they sit in collapsed/supporting context rather than competing with the feed, especially on mobile.
- Activity has now been volume-tested with a 20-item representative monitoring intake covering ticketed play, Black Lotus, site-tree changes, artist/store/prize/map opportunities, receipts, travel, badge mail, vendor radar, and quiet source checks. Home deliberately counts and surfaces only hot Home-destination findings; quieter notice items remain reviewable in Activity; quiet/no-action observations default to reviewed so routine checks do not create false urgency.
- Primary surfaces are now URL-addressable by hash. Home, Calendar, Plan, Explore, Map, Wallet, Trip, Artists, Notes, and Activity can be loaded directly, survive refresh, and participate in browser back/forward. Monitoring alerts can now target a stable surface URL without dumping the owner back at Home.
- Artists is backed by the canonical Supabase catalog and includes confirmed Atlanta appearances. Artist identity, directory/list information, and confirmed appearances may be visible to authenticated companions. The card database, owned printings, signing picks/interests, private card images, and signing workbench remain Kavi-only unless Kavi explicitly changes that boundary.
- The preview PWA now auto-updates its service worker and claims the latest build. This is intentionally review-friendly: the public GitHub Pages POC should not keep serving an old shell after a deploy when Kavi is checking the app on iPhone.
- Monitor-discovered objects now open through a more destination-aware shared detail layer. Activity findings still preserve source/retrieval/review context, but Wallet findings present as Proof, Trip and Map findings as Place, Explore/Calendar findings as Event, Artists findings as Artist, and Notes findings as Note. This keeps the monitoring lane from feeling like a pile of generic log records.
- The first v1.5 no-dead-control cleanup pass is complete. Wallet Play no longer presents fake clickable future receipts before ticketed play exists; representative play proof rows now open their proof/code/arrival surfaces; Wallet Other's Trip actions route to Trip instead of explanation modals; the Leap order action is a real external link; Notes no longer shows a dead New Note button; and Explore uses "Compare in Plan" instead of overstating the bracketed Plan study.
- The fixture-backed app now hydrates the known official Atlanta Black Lotus schedule beyond the single Planechase trust slice. Explore includes Thursday early-access items, the Progressive Sealed league, Welcome Reception, Friday under-construction play event, Saturday Planechase, Sunday Mystery Booster 2 drafts, and Sunday feedback session. Calendar now surfaces Thursday, Friday, Saturday, and Sunday Black Lotus schedule anchors with explicit source uncertainty: locations are TBD and the schedule remains subject to change. This is legitimate official-page information presented in the POC UI, not a broad canonical ingestion pipeline or new schema.
- The monitoring surfaces are accepted: Home remains reserved for hot owner-relevant signals and Activity is the reviewed inbox. The original file-backed intake is now a legacy preview seam; real authenticated surveyor evidence uses the Supabase review queue. Do not reopen placeholder UI polish.
- `pnpm validate:monitoring` now guards `public/monitoring-intake.json` before any preview publication. It verifies required fields, allowed source/severity/destination enums, duplicate IDs, lowercase kebab-case IDs, quiet findings accidentally routed to Home, and obvious private artifact leakage patterns. The first run caught an unsafe public fixture reference to a Delta confirmation value, which was sanitized to a generic Delta confirmation object label.
- The MagicCon Black Lotus VIP Discord is now approved as a bounded high-signal radar source, especially staff-answer and Black Lotus discussion channels. Discord observations route through Activity/Sources/Changes and affected objects, with ordinary community chatter kept non-canonical unless supported by staff wording, official links, inbox evidence, or later first-party confirmation. Kavi is `Metavirus`; Chris is `Gremmy`, but public preview fixtures should minimize usernames unless identity affects interpretation.
- The reference app's Discord method has been distilled without importing its broad schema: use a tiny watch map, explicit safe access mode, channel purpose, expected signal types, cursors, and per-run quiet/useful state. MagicCon starts with manual screenshot/open-channel review; automated Discord browsing stays parked until a dedicated read-only profile and mechanical guards are explicitly approved.
- Official Wizards Magic news is now allowed only as a narrowly filtered product/context source. It can explain event products such as Mystery Booster or Festival in a Box, but the monitor must not become a general Magic news reader.
- Activity has received its planned closeout and now also renders the Supabase-backed Kavi decision queue. Further Activity tweaks should be driven by real findings or phone defects, not by reopening fixture polish.
- Plan-lite received a final 1.5 usefulness pass around the known Black Lotus schedule. It now exposes a compact status strip for official Black Lotus anchors, promoted contenders, and watch-sensitive/TBD items; rows carry tighter time chips and stronger BL/source-watch signals. This keeps Plan legitimate for the quiet period without pretending the real ticketed-play contention engine exists yet.
- The fixture-backed 1.5 mobile review is accepted. Kavi flipped through the public preview on iPhone on August 8 and reported it looks good. Treat Home, Activity, Wallet, Plan-lite, Explore, Calendar, Trip, Map, Artists, and Notes as sufficiently coherent for the current tranche; further UI tweaks should be defect- or real-data-driven rather than exploratory polish.
- A small post-acceptance mobile navigation correction is now in place: the top-left header control opens a left-side main navigation drawer on phone instead of presenting a strange isolated back affordance. Desktop keeps the direct rail and back affordance behavior. This is a usability fix, not a reopening of broad 1.5 UI polish.
- The mobile main drawer has been tightened again for phone use. It now uses a narrower left-overlay width and a simpler three-row grid (`brand -> nav -> activity`) so it reads more like the desktop rail and leaves less dead air on iPhone.
- Home and Activity now share one ranked activity model instead of separate monitoring, selection, and notes presentations. `Hot` remains interruption-quality: consequential publisher changes and hard commitments can qualify, while Prize Tix adjustments and ordinary notes do not. Home's single `Worth knowing` lane admits a moderate set of recent, unread Home-worthy context—including grouped note bursts—without repeating the same signal in status, priority, and activity cards. One fresh Hot item may be featured for 48 hours; it then falls into the bounded recent-signal scroller. Hot context ages off Home after seven days and ordinary context after three, while Activity retains the durable history. Activity `All` uses those same grouped items; the dedicated `Notes` lane retains the complete individual note index.
- The Home landing composition has been tightened after visual review. The top row now separates a short attention summary from the next expected milestone, while the lower row keeps the bounded `Worth knowing` signal scroller beside useful planning context. Do not reopen Home for speculative dashboard polish unless real data or a visible defect exposes a new need.
- Inbox `@`-mention ticklers now route to the source context rather than opening an intermediate note/activity drawer. Receipt-note mentions land on the relevant Wallet proof context, event-note mentions land on the event surface, trip/hotel mentions land on Trip context, and notes-cluster mentions land in Notes context. Inbox is a notification/tickler surface, not a separate note-content presentation layer.
- Explore, Plan, and Calendar now share the same event-detail direction: desktop keeps the selected event in a persistent right-side inspector/drawer aligned to the active list/board context, while mobile uses a pullout/bottom-sheet style detail layer. Explore exposes Interested / Tentative / Committed controls in the detail panel like Plan, and the obsolete nested `Open object detail` footer has been removed so event details do not spawn a second generic object drawer.
- Authenticated continuity refresh is no longer all-or-nothing. Notes, selections, and activity history load independently, so one unavailable resource cannot erase the others or falsely report a selection failure. A fully successful refresh clears stale continuity errors. Recent API readback confirmed HTTP 200 for all three canonical Supabase tables.
- The August 21 reliability checkpoint is closed. GitHub CLI operations use the ignored repo-local `.codex-local/gh` credential lane, `pnpm gh:auth-local` provisions or verifies it, and `pnpm readiness` proves the expected `metavirus` GitHub identity plus canonical Supabase project before consequential work. Supabase selections and activity reads now refresh an expired session and retry exactly once after a 401; focused tests prevent that recovery behavior from silently regressing. Recurrence should use these documented lanes rather than another ad hoc login or broad error banner.
- A final August 9 copy-cleanup pass removed more prototype-sounding language from Explore, Activity, Artists, Plan provenance, and Wallet proof surfaces so the shared preview reads more like a real tool and less like a design artifact explaining itself.
- The immediate operational frontier is completing the surveyor closed loop: configure the GitHub Actions server credential, prove clean staging/dedupe, then ship and verify. Ticketed-play readiness remains the next product-data frontier after that bounded gate.
- Ticketed-play hydration now has an accepted contract in `docs/TICKETED_PLAY_HYDRATION_CONTRACT.md` plus a TypeScript boundary in `src/lib/ticketedPlayHydration.ts`. LEAP is a one-way public inventory/availability hydrator, not a co-equal planning system: ingest all events when Atlanta listings appear, preserve raw source snapshots, normalize Explore/Plan/Calendar dimensions, group first-drop and later change signals to avoid Home floods, keep purchased/registered proof separate from public inventory, and let the app own planning state.
- The GitHub Pages publication contract is now source-branch based. `pnpm publish:pages` is only a local artifact/preflight command; the actual public deploy is the GitHub Actions Pages workflow after a source push. Public review requires waiting for the Pages workflow and then running `pnpm verify:public`, which prepares its own Pages-stamped local comparison artifact before checking the cache-busted public URL.
- The repo now has an explicit anti-waste operating mode and known-gremlins list. Small UI/copy/layout changes use the fast `pnpm build` lane plus required viewport verification when visual; broader public preview UI changes use `pnpm check:ship`, source push, Actions deploy, and public verification; Supabase/auth/data/research/monitoring changes still use the full readiness and trust gate.
- GitHub Pages deploy verification now has one known nuisance: after a correct source push and green Pages workflow, the public URL can briefly keep serving an older JS bundle because of cache/service-worker lag. Use `pnpm verify:public` and a cache-busted URL before claiming the public app is current; live browser confirmation may still need a refresh, reopen, or short wait.
- Execution discipline has been hardened after the August 10-11 workflow/runtime cleanup failure: operational mistakes that create red CI/deploy signals or user-visible breakage must be named plainly, while known sandbox/network/cache gremlins should be routed through documented paths without rediscovery or theatrical narration. The fix is stricter anticipation and one-variable operational changes, not heavier blanket ceremony.
- The deterministic web-watch lane uses `monitoring/watch-set.json`, the narrow Gmail query map, and `pnpm monitor`. On August 21 it reported seven changed watched sources with zero fetch failures, dominated by shared Atlanta navigation exposing Magic Play/Ticketed Play/Prize links. The changes were inspected; no baseline was accepted or modified.
- The cloud surveyor remains narrow—public watch URLs only, no Gmail/Discord/LEAP—but its local unpushed successor now stages fingerprint-deduplicated noncanonical evidence into `monitoring_findings`. It fails closed until GitHub Actions has `SUPABASE_SECRET_KEY`.
- A pure trust-slice reconciliation path now proves the intended fail-safe semantics in tests: a revision must be a new observation that explicitly supersedes the current observation; the prior observation remains recoverable; supported normalized fields may change; and personal decision plus itinerary remain untouched until owner review. A live reviewed write/readback is still required.
- Live verification proves the owner can read all five records while another authenticated identity reads none and cannot update the decision or delete the itinerary. Schema lint is clean. Supabase advisors report only the existing account-level warning that leaked-password protection is disabled.

## Next safe lanes

1. Configure GitHub Actions repository secret `SUPABASE_SECRET_KEY` with a modern server-only key for `pavjsexxbueuzhzgemgy`; never expose it to browser/Vite code or chat.
2. Manually run the surveyor, prove one changed report stages a candidate, rerun the same report to prove fingerprint/occurrence dedupe, and verify the Kavi/non-Kavi RLS boundary.
3. Run tests/build/readiness/validation/advisors and desktop/mobile Activity/Home inspection; then commit, push, wait for clean Actions/Pages, run `pnpm verify:public`, and inspect the deployed flow.
4. Keep the preview stable unless real defects appear; confirm Google OAuth/PWA and critical offline read behavior on a real device.
5. Define private Storage for badge/travel/receipt originals.
6. Keep Plan-lite unless purchased/locked events create real conflicts; resume the production contention engine only when conflict resolution becomes genuinely urgent.
7. If Map work is prioritized before the official artifact, execute only `MAP-NOW-01` through `MAP-NOW-03`: freeze the versioned spatial contract, quarantine historical/synthetic fixtures, and prove the leakage guard. Keep the live Map as Trip-area orientation plus official-artifact receiver; do not begin any `MAP-ARR-*` task until first-party Atlanta 2026 evidence passes the arrival identity gate.

## Active boundaries

No large data ingest, speculative comprehensive schema, full production hosting, offline writes, or unreviewed agent promotion. Supabase remains canonical; the temporary GitHub Pages build is only a fixture-backed review preview.
